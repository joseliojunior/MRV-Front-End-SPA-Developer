const v1 = parseInt(gets());
const v2 = parseInt(gets());
const v3 = parseFloat(gets());
const salary = parseFloat(v2 * v3).toFixed(2);
console.log(`NUMBER = ${v1}`);
console.log(`SALARY = U$ ${salary}`);